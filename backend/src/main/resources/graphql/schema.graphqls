type Product {
    id: ID!
    name: String!
    price: Float!
    category: String
    imageUrl: String
    stock: Int!
    detailedDescription: String
}

type Query {
    products: [Product]!
    productById(id: ID!): Product
}