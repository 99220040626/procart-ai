{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Launch ProCart Backend",
            "request": "launch",
            "cwd": "${workspaceFolder}/backend",
            "mainClass": "com.procart.EcommerceBackendApplication",
            "envFile": "${workspaceFolder}/backend/.env"
        }
    ]
}